# EXAMPLE CONFIGURATION FILE
# Copy this file to ".env" and adjust values.

# --- Database ---
DB_NAME=local_trade
DB_USER=postgres
DB_PASSWORD=postgres
# Use 'localhost' for local run, 'postgres' for docker-compose
DB_HOST=localhost

# --- Security ---
JWT_SECRET=change_me_to_something_secure_hs256

# --- Redis ---
# Use 'localhost' for local run, 'redis' for docker-compose
REDIS_HOST=localhost
REDIS_PORT=6379

# --- RabbitMQ ---
# Use 'localhost' for local run, 'rabbitmq' for docker-compose
RABBITMQ_HOST=localhost

# --- S3 / MinIO ---
# Use 'localhost' for local run, 'minio' for docker-compose internal connection
MINIO_HOST=localhost

# Credentials
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_ENDPOINT=http://localhost:9000
S3_ACCESSKEY=minioadmin
S3_SECRETKEY=minioadmin
AWS_REGION=eu-central-1

# --- Network / IP ---
# Your local LAN IP (e.g. 192.168.1.X) for phone testing, or 'localhost'
APP_HOST=localhost

# --- Mailtrap ---
MAILTRAP_USERNAME=your_username
MAILTRAP_PASSWORD=your_password